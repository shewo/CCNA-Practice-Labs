SW1>en
SW1#conf t
Enter configuration commands, one per line.  End with CNTL/Z.
SW1(config)#int range g1/0/2 - 3
SW1(config-if-range)#switchport mode ac
SW1(config-if-range)#switchport access vlan 10
% Access VLAN does not exist. Creating vlan 10
SW1(config-if-range)#
SW1(config-if-range)#switch %SPANTREE-2-RECV_PVID_ERR: Received 802.1Q BPDU on non trunk GigabitEthernet1/0/2 VLAN1.

%SPANTREE-2-BLOCK_PVID_LOCAL: Blocking GigabitEthernet1/0/2 on VLAN0001. Inconsistent port type.

%SPANTREE-2-RECV_PVID_ERR: Received 802.1Q BPDU on non trunk GigabitEthernet1/0/3 VLAN1.

%SPANTREE-2-BLOCK_PVID_LOCAL: Blocking GigabitEthernet1/0/3 on VLAN0001. Inconsistent port type.


SW1(config-if-range)#swithchport voice vlan 20
                         ^
% Invalid input detected at '^' marker.
	
SW1(config-if-range)#switchport voice vlan 20
% Voice VLAN does not exist. Creating vlan 20
SW1(config-if-range)#int g1/0/1
SW1(config-if)#
SW1(config-if)#switchport trunk encapsulation dot1q
                                ^
% Invalid input detected at '^' marker.
	
SW1(config-if)#switchport trunk encapsulation dot1q
                                ^
% Invalid input detected at '^' marker.
	
SW1(config-if)#switchport trunk encapsulation dotlq
                                ^
% Invalid input detected at '^' marker.
	
SW1(config-if)#switchport trunk encapsulation dot1q
                                ^
% Invalid input detected at '^' marker.
	
SW1(config-if)#switchport trunk encapsulation dot1q
                                ^
% Invalid input detected at '^' marker.
	
SW1(config-if)#sw
SW1(config-if)#switchport trunk encapsulation dot1q
                                ^
% Invalid input detected at '^' marker.
	
SW1(config-if)#switchport trunk
% Incomplete command.
SW1(config-if)#switchport mode trunk
SW1(config-if)#switchport trunk allowed vlan 10,20
SW1(config-if)#
%LINK-5-CHANGED: Interface GigabitEthernet1/0/1, changed state to up

%LINEPROTO-5-UPDOWN: Line protocol on Interface GigabitEthernet1/0/1, changed state to up
